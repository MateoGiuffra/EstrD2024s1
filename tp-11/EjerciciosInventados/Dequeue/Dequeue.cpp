#include "Deque.h"
#include <stdlib.h>

// Crea una deque vacía
Deque* createDeque() {
    // Crea una estructura Deque inicializando los campos a valores adecuados.
    // Eficiencia: O(1)
}

// Libera la memoria de la deque
void destroyDeque(Deque* dq) {
    // Libera toda la memoria asignada a la deque, incluido cada nodo.
    // Eficiencia: O(n), donde n es el número de elementos en la deque.
}

// Inserta un elemento al frente de la deque
void insertFront(Deque* dq, int data) {
    // Inserta un nodo con el valor 'data' al frente de la deque.
    // Eficiencia: O(1)
}

// Inserta un elemento al final de la deque
void insertRear(Deque* dq, int data) {
    // Inserta un nodo con el valor 'data' al final de la deque.
    // Eficiencia: O(1)
}

// Elimina un elemento del frente de la deque
void deleteFront(Deque* dq) {
    // Elimina el nodo al frente de la deque.
    // Eficiencia: O(1)
}

// Elimina un elemento del final de la deque
void deleteRear(Deque* dq) {
    // Elimina el nodo al final de la deque.
    // Eficiencia: O(1)
}

// Obtiene el elemento del frente de la deque
int getFront(Deque* dq) {
    // Devuelve el valor del nodo al frente de la deque.
    // Eficiencia: O(1)
}

// Obtiene el elemento del final de la deque
int getRear(Deque* dq) {
    // Devuelve el valor del nodo al final de la deque.
    // Eficiencia: O(1)
}

// Comprueba si la deque está vacía
int isEmptyDeque(Deque* dq) {
    // Devuelve 1 si la deque está vacía, 0 en caso contrario.
    // Eficiencia: O(1)
}

// Obtiene el tamaño de la deque
int getSizeDeque(Deque* dq) {
    // Devuelve el número de elementos en la deque.
    // Eficiencia: O(1)
}
